//! Star Fight client. M2 adds the Bevy app (board, placement, maneuver
//! previews); until then this is a compile-checked placeholder.

fn main() {
    println!(
        "sf-client (protocol v{}) — Bevy app arrives in milestone M2",
        sf_proto::PROTOCOL_VERSION
    );
}
